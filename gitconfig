[user]
	name = McCabe, Robert J
	email = Robert.McCabe@rockwellcollins.com
[sendemail]
    smtpserver = notessmtp.rockwellcollins.com
    smtpserverport = 25
[core]
    pager = less -X
    excludesFile =
[color]
    diff = always
    status = always
    branch = always
    interactive = always
    ui = true
    pager = true
[alias]
    dt = difftool --dir-diff
    #Shows the commit corresponding to the merge of branch $1 into branch $1
    aftermerge = "!f() { git rev-list --ancestry-path $1 --not $2 | tail -1; }; f"
    justbranch = "!f() { git rev-list $1 --not ${2}^; }; f"
    #pl = log --graph --format=oneline --abbrev-commit --decorate --all
    pl = log --graph --format=oneline --abbrev-commit --decorate
[diff]
	tool = bc3
[difftool]
    prompt = false
[merge]
	tool = bc3
[mergetool]
	bc3 = trustExitCode
[http]
	sslverify = false
[oh-my-zsh]
	hide-status = 1
[difftool "bc3"]
	cmd = \"c:/Program Files/Beyond Compare 3/BCompare.exe\" "$(cygpath -w $LOCAL)" "$(cygpath -w $REMOTE)"
[mergetool "bc3"]
	cmd = \"c:/Program Files/Beyond Compare 3/BCompare.exe\" "$(cygpath -w $LOCAL)" "$(cygpath -w $REMOTE)" "$(cygpath -w $BASE)" /mergeoutput="$(cygpath -w $MERGED)"
